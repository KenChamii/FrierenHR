{
  // Placeholder — fill in (or override via environment variables / hosting platform config)
  // once you know the real production frontend URL. Don't leave this pointing at localhost.
  "Cors": {
    "AllowedOrigins": [ "https://your-production-frontend-domain.example" ]
  },

  "Logging": {
    "LogLevel": {
      "Default": "Warning",
      "Microsoft.AspNetCore": "Warning"
    }
  }
}
